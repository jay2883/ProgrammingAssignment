User can run the executable and choose one of the 3 program to run
Once the program is selected, user needs to input the data. Selected program runs and displays the output.
Once the output is displayed, it will ask user if wish to continue to run another program.
